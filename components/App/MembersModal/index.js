// @flow
import React from 'react'

// Local
import Modal from '../../shared/Modal'

type Props = {
  onClose: () => void,
}

export default (props: Props) => (
  <Modal width={750} {...props}>
    sasdasd
  </Modal>
)
